# MainColor

This is a binary to get the average color of a Jpeg/Png image. The usage is like this: `main-color -png "file.png"` if the file is a png image use -png, if it is a jpeg image. use: `main-color -jpeg "file.jpeg"`